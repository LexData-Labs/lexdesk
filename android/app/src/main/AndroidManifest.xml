<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location is REQUIRED to read SSID/BSSID on Android 8.1+. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Per-org SUPER_ADMIN can enable background location for geofence /
         periodic / continuous modes. Without this permission the app can
         only request "While using the app" at the system prompt. -->
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:targetApi="29" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Android 13+: required for nearby WiFi info APIs.
         We DO use this for location correlation, so we do NOT set neverForLocation. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:targetApi="33" />

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Foreground service permissions for the continuous-location mode.
         FOREGROUND_SERVICE_LOCATION is REQUIRED on API 34 to start an FGS
         typed as "location" — without it startForeground() throws. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_LOCATION"
        tools:targetApi="34" />

    <!-- Re-register geofences and re-start the continuous-tracking service
         after device reboot or app update. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Required on Android 13+ to surface geofence / FGS notifications. -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendDesk"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AttendDesk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Hands a content:// URI for camera-captured profile photos to
             external camera apps via TakePicture. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Continuous-mode foreground service: streams location while installed
             (24/7 per the org's continuousIntervalSeconds). Persistent
             notification is always visible while it runs. -->
        <service
            android:name=".location.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Geofence ENTER/EXIT receiver. Fires either an auto check-in
             (when verify.face is disabled for the org) or a notification
             prompting the user to check in. -->
        <receiver
            android:name=".location.GeofenceReceiver"
            android:exported="false" />

        <!-- Re-registers geofences and restarts the continuous FGS after
             device reboot or app update. -->
        <receiver
            android:name=".location.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
